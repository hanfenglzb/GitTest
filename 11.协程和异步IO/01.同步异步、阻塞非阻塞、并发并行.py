# 并发
# 一个CPU在一个时间段内可以并发执行多个程序，但是同一时刻还是只能运行一个

# 并行
# 在同一个时刻有多个程序运行（有几个CPU就能并行几个程序）

# 同步
# 同步是指碰到IO操作，会等待IO完成之后再继续执行。

# 异步
# 异步是指碰到IO操作时，不等待IO完成，任务完成之后主动通知CPU（中断方式）。

# 阻塞
# 阻塞是指在调用一个函数时，进程或线程会被挂起等着数据的返回

# 非阻塞
# 非阻塞是指...调用一个任务时，不管有没有数据都立即返回。

